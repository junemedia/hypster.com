@model List<hypster_tv_DAL.AolSeedVideo>
@{
    Layout = null;
}



@foreach (var item in Model)
{   
    <a class="ppSeedsVC" href="/relatedVideos?ss=@ViewBag.ss&al=@item.id" target="_blank">
        <img alt="" src="@item.image" />
        <div>@item.title</div>
    </a>
}