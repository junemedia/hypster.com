<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta id="vpM" name="viewport" content="width=device-width, initial-scale=1, user-scalable=0">    
    <title>@ViewBag.Title</title>
    <meta name="alexaVerifyID" content="D1zuW6QmyfkVJqITiYkg7ecUz5k" />
    <meta name="description" content="@ViewBag.Desc" />
    <link rel="shortcut icon" href="/favicon.ico" type="image/x-icon">
    <link rel="icon" href="/favicon.ico" type="image/x-icon">

    @if (ViewBag.LinkTag != null)
    {
        <text>@Html.Raw(ViewBag.LinkTag)</text>
    }

    @if (Request.Cookies["cssS"] != null && Request.Cookies["cssS"].Value != "")
    {
        Console.Write(Request.Cookies["cssS"].ToString());
        <link id="mStyles1" href="/areas/apps/css/appSite@(Request.Cookies["cssS"].Value).css?id=24007210301" rel="stylesheet" type="text/css" async />
    }
    else
    {
        <link id="mStyles1" href="/areas/apps/css/appSite2.css?id=24007210301" rel="stylesheet" type="text/css" async />
    }

    <link src="http://code.jquery.com/ui/1.10.1/themes/base/jquery-ui.css"  rel="stylesheet" type="text/css" />
    <script src="http://code.jquery.com/jquery-1.8.3.min.js"  type="text/javascript"></script>
    <script src="http://code.jquery.com/ui/1.10.1/jquery-ui.js"  type="text/javascript"></script>
    <script src="http://ajax.aspnetcdn.com/ajax/mvc/3.0/jquery.unobtrusive-ajax.min.js" type="text/javascript"></script>

    @Scripts.Render("~/bundlesApps/scripts")

    <script type="text/javascript">
        var check_width = 0;
        var check_height = 0;
        $(document).ready(function () {
            var curr_width = $(window).width();
            var randomnumber = Math.floor(Math.random() * 1100000);
            check_width = $(window).width();
            check_height = $(window).height();
            $("#cdc1").css("opacity", "1");
            if (curr_width > 1024)
            {
                $("#mStyles1").attr("href", "/areas/apps/css/appSite2.css?id=" + randomnumber);
                eraseCookie("cssS");
                createCookie("cssS", "2", 1);
            }
            else
            {
                if (curr_width < 420)
                {
                    $("#mStyles1").attr("href", "/areas/apps/css/appSite0.css?id=" + randomnumber);
                    eraseCookie("cssS");
                    createCookie("cssS", "0", 1);
                }
                else if (curr_width >= 420 && curr_width <= 650)
                {
                    $("#mStyles1").attr("href", "/areas/apps/css/appSite05.css?id=" + randomnumber);
                    eraseCookie("cssS");
                    createCookie("cssS", "05", 1);
                }
                else
                {
                    $("#mStyles1").attr("href", "/areas/apps/css/appSite1.css?id=" + randomnumber);
                    eraseCookie("cssS");
                    createCookie("cssS", "1", 1);
                }
            }
        });

        $(window).resize(function () {
            if ($(window).width() == check_width) { // && $(window).height() == check_height) {
                return;
            }
            var curr_width = $(window).width();
            var randomnumber = Math.floor(Math.random() * 1100000);
            if (curr_width > 1024)
            {
                $("#cdc1").css("opacity", "0");
                setTimeout(function () { $("#cdc1").css("opacity", "1"); }, 1000);

                $("#mStyles1").attr("href", "/areas/apps/css/appSite2.css?id=" + randomnumber);
                eraseCookie("cssS");
                createCookie("cssS", "2", 1);
            }
            else
            {
                if (curr_width < 420)
                {
                    $("#cdc1").css("opacity", "0");
                    setTimeout(function () { $("#cdc1").css("opacity", "1"); }, 1000);

                    $("#mStyles1").attr("href", "/areas/apps/css/appSite0.css?id=" + randomnumber);
                    eraseCookie("cssS");
                    createCookie("cssS", "0", 1);
                }
                else if (curr_width >= 420 && curr_width <= 650)
                {
                    $("#cdc1").css("opacity", "0");
                    setTimeout(function () { $("#cdc1").css("opacity", "1"); }, 1000);

                    $("#mStyles1").attr("href", "/areas/apps/css/appSite05.css?id=" + randomnumber);
                    eraseCookie("cssS");
                    createCookie("cssS", "05", 1);
                }
                else
                {
                    $("#cdc1").css("opacity", "0");
                    setTimeout(function () { $("#cdc1").css("opacity", "1"); }, 1000);

                    $("#mStyles1").attr("href", "/areas/apps/css/appSite1.css?id=" + randomnumber);
                    eraseCookie("cssS");
                    createCookie("cssS", "1", 1);
                }
            }
            check_width = $(window).width();
            check_height = $(window).height();
        });
    </script>
</head>

<body>
    <div id="cdc1" style="opacity:0;">
        <div class="MainBD13">
            <div class="shrTopC1">
                <span>@Html.Partial("~/Views/hypAds/ad_320x50_atf.cshtml")</span>
            </div>
            <div class="ContentHeaderCont">
                @Html.Partial("~/Areas/apps/Views/Shared/pr_TopMenu.cshtml")
            </div>
            <div class="ContentRootCont" >
                 @RenderBody()
            </div>    
            <div class="push"></div>
        </div>    
        <div class="ContentFooterCont">
            @Html.Partial("~/Areas/apps/Views/Shared/pr_BottomFooter.cshtml")
            <div class="shrBottomC1">
                <span>@Html.Partial("~/Views/hypAds/ad_320x50_btf.cshtml")</span>
            </div>
            <div style="clear:both"></div>
        </div>
        <div class="Trans_BACK" id="HideBackDiv" onclick="Show_Content();"></div>
        <div id="popupContainer"></div>

        <script type="text/javascript">
            //var _gaq = _gaq || [];
            //_gaq.push(['_setAccount', 'UA-28695705-1']);
            //_gaq.push(['_setDomainName', 'hypster.com']);
            //_gaq.push(['_trackPageview']);

            //(function () {
            //    var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true;
            //    ga.src = ('https:' == document.location.protocol ? 'https://' : 'http://') + 'stats.g.doubleclick.net/dc.js';
            //    var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(ga, s);
            //})();
            (function (i, s, o, g, r, a, m) {
                i['GoogleAnalyticsObject'] = r; i[r] = i[r] || function () {
                    (i[r].q = i[r].q || []).push(arguments)
                }, i[r].l = 1 * new Date(); a = s.createElement(o),
                m = s.getElementsByTagName(o)[0]; a.async = 1; a.src = g; m.parentNode.insertBefore(a, m)
            })(window, document, 'script', 'https://www.google-analytics.com/analytics.js', 'ga');
            ga('create', 'UA-28695705-1', 'auto', { 'name': 'prodTracker', 'legacyCookieDomain': 'hypster.com' });
            ga('prodTracker.send', 'pageview');
            ga('create', 'UA-99868480-1', 'auto', { 'name': 'stageTracker' });
            ga('stageTracker.send', 'pageview');
        </script>
        </div>
    </body>
</html>
