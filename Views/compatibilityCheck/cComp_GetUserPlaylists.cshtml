@model hypster.ViewModels.Compatibility_UserPlaylists_ViewModel
@{
    Layout = null;
}



<div style=" float:left; width:100%;">
    
    <select id="ChoosePlaylist" name="ChoosePlaylist" onchange="change_UserComp_playlist(this);">
        @foreach (var item in Model.playlists_list)
        {
            <option value="@item.id" @if(ViewBag.ActivePlaylist == item.id){<text>selected="selected"</text>}>@item.name</option>
        }
    </select>

    <div id="UserCompPlstHolder1">
    </div>

</div>


<script type="text/javascript">
    @if (ViewBag.US_ID != null)
    {
        <text>var comp_tool_user_id =</text> @ViewBag.US_ID <text>;</text>
    }
    else
    {
        <text>var comp_tool_user_id = undefined;</text>
    }
</script>
